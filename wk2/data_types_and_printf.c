#include <stdio.h>
#include <stdlib.h>

int main() {

    int age = 40; // integers
    double gpa = 3.7; // decimal numbers

    char grade = 'A'; // single character with SINGLE QUOTATION MARKS
    char phrase[] = "arknights rock"; // creates a string of characters (array)


    // When wanting to print double quotes, use \ before quotations
    printf("Hello \"Mr.NiceGuy\", how's it going?\n");

    return 0;
}
